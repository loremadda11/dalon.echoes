---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Dalon">
  <!-- Navigation -->
  <nav class="nav">
    <div class="nav-content">
      <img src="/logo.png" alt="DALON" class="logo-img">
      <div class="nav-links">
        <a href="#about">ABOUT</a>
        <a href="#music">MUSIC</a>
        <a href="#contact">CONTACT</a>
      </div>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-image">
      <img src="/dalon.png" alt="Dalon" />
    </div>
    <div class="hero-text">
      <h1>HI I'M DALON</h1>
      <p>In the absurdity of the world, I've always seen art as total and unique expression - there are no other ways. Music is the most evocative part. I've always listened to music, and for a few years now I've been passionate about producing it and throwing out thoughts.</p>
      <p class="location">Based in Tuscany</p>
    </div>
  </section>

  <!-- What I Do Section -->
  <section class="what-i-do" id="music">
    <h2>WHAT I SHARE</h2>
    
    <div class="grid">
      <div class="card">
        <h3>TECHNO</h3>
        <p>Exploring rhythm, space and tension through electronic soundscapes.</p>
        <a href="#" class="btn">LISTEN</a>
      </div>
      
      <div class="card">
        <h3>AMBIENT</h3>
        <p>Atmospheric textures and evolving sonic environments for contemplation.</p>
        <a href="#" class="btn">LISTEN</a>
      </div>
      
      <div class="card">
        <h3>THOUGHTS</h3>
        <p>Reflections on sound, process, and the creative journey.</p>
        <a href="#" class="btn">READ</a>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="contact" id="contact">
    <h2>CONNECT</h2>
    <div class="social-links">
  <a href="https://www.instagram.com/dalon_echoes/" target="_blank">INSTAGRAM</a>
  <a href="https://youtube.com/@tuocanale" target="_blank">YOUTUBE</a>
  <a href="#" target="_blank">SOUNDCLOUD</a>
</div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <p>© 2026 DALON - Based in Tuscany</p>
  </footer>
</Layout>

<style>
  /* Navigation */
  .nav {
    padding: 2rem 4rem;
    border-bottom: 1px solid rgba(0,0,0,0.1);
  }

  .nav-content {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 800;
    letter-spacing: 0.1em;
  }

  .logo-img {
  height: 2.5rem; /* Regola altezza come preferisci */
  width: auto;
}

  .nav-links {
    display: flex;
    gap: 3rem;
    font-size: 0.9rem;
    letter-spacing: 0.05em;
  }

  /* Hero Section */
  .hero {
    max-width: 1400px;
    margin: 0 auto;
    padding: 8rem 4rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
  }

  .hero-image {
    border-radius: 24px;
    overflow: hidden;
    aspect-ratio: 1;
  }

  .placeholder-image {
    width: 100%;
    height: 100%;
    background: var(--bg-secondary);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
    color: var(--text-secondary);
    letter-spacing: 0.1em;
  }

  .hero-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

  .hero-text h1 {
    font-size: 4rem;
    margin-bottom: 2rem;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }

  .hero-text p {
    font-size: 1.1rem;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
    line-height: 1.8;
  }

  .location {
    font-size: 0.9rem !important;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-primary) !important;
    font-weight: 500;
    margin-top: 3rem !important;
  }

  /* What I Do Section */
  .what-i-do {
    max-width: 1400px;
    margin: 0 auto;
    padding: 8rem 4rem;
  }

  .what-i-do h2 {
    font-size: 3rem;
    text-align: center;
    margin-bottom: 4rem;
    padding-bottom: 2rem;
    border-bottom: 2px solid var(--text-primary);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
  }

  .card {
    padding: 3rem;
    background: var(--bg-secondary);
    border-radius: 16px;
    transition: transform 0.3s ease;
  }

  .card:hover {
    transform: translateY(-8px);
  }

  .card h3 {
    font-size: 1.8rem;
    margin-bottom: 1rem;
    letter-spacing: 0.05em;
  }

  .card p {
    color: var(--text-secondary);
    margin-bottom: 2rem;
    line-height: 1.7;
  }

  .btn {
    display: inline-block;
    padding: 0.75rem 2rem;
    border: 2px solid var(--text-primary);
    border-radius: 8px;
    font-size: 0.85rem;
    letter-spacing: 0.1em;
    font-weight: 500;
    transition: all 0.3s ease;
  }

  .btn:hover {
    background: var(--text-primary);
    color: var(--bg-primary);
    opacity: 1;
  }

  /* Contact Section */
  .contact {
    max-width: 1400px;
    margin: 0 auto;
    padding: 6rem 4rem;
    text-align: center;
  }

  .contact h2 {
    font-size: 3rem;
    margin-bottom: 3rem;
  }

  .social-links {
    display: flex;
    justify-content: center;
    gap: 3rem;
    font-size: 1.1rem;
    letter-spacing: 0.05em;
  }

  .social-links a {
    position: relative;
  }

  .social-links a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--text-primary);
    transition: width 0.3s ease;
  }

  .social-links a:hover::after {
    width: 100%;
  }

  .social-links a:hover {
    opacity: 1;
  }

  /* Footer */
  .footer {
    padding: 3rem 4rem;
    text-align: center;
    border-top: 1px solid rgba(0,0,0,0.1);
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  /* Responsive */
  @media (max-width: 968px) {
    .nav {
      padding: 1.5rem 2rem;
    }

    .nav-links {
      gap: 1.5rem;
      font-size: 0.8rem;
    }

    .hero {
      grid-template-columns: 1fr;
      padding: 4rem 2rem;
      gap: 3rem;
    }

    .hero-text h1 {
      font-size: 2.5rem;
    }

    .grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .what-i-do, .contact {
      padding: 4rem 2rem;
    }

    .what-i-do h2, .contact h2 {
      font-size: 2rem;
    }

    .social-links {
      flex-direction: column;
      gap: 1.5rem;
    }
  }
</style>